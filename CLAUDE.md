# CLAUDE.md — oxicrypt

> **`AGENTS.md` is the canonical, model-agnostic source of session rules** for this repository
> (the cross-tool standard read natively by Codex, Cursor, Gemini CLI, and others). Project context,
> key paths, session bootstrap, canonical homes, definition of done, doc-sync discipline, and license
> posture all live there — not here.
>
> This file exists only because **Claude Code auto-loads `CLAUDE.md`, not `AGENTS.md`.** The import
> below pulls the canonical rules in at session start. Do not duplicate content here — edit `AGENTS.md`.

@AGENTS.md
